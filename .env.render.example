# 📝 Exemplo de Variáveis de Ambiente para Render

# ====================================
# VARIÁVEIS OBRIGATÓRIAS NO RENDER
# ====================================

# URL de conexão com PostgreSQL (use a INTERNAL URL do Render)
# Formato: postgresql://username:password@host:port/database
# Exemplo: postgresql://etica123:CHeeJkYLvfJCoFEWq7fgo7SlC6TO4Z4n@dpg-d48hefu3jp1c73cjb9qg-a/etica_vendas
DATABASE_URL=postgresql://usuario:senha@host/database

# Segredo para assinar tokens JWT (gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=seu_segredo_de_32_caracteres_ou_mais_aqui

# ====================================
# VARIÁVEIS OPCIONAIS
# ====================================

# Ambiente (production para o Render)
NODE_ENV=production

# Porta (o Render define automaticamente, não precisa configurar)
# PORT=10000

# CORS - Ative apenas se frontend estiver em domínio diferente
CORS_ENABLED=false

# Origem permitida no CORS (se CORS_ENABLED=true)
# FRONTEND_ORIGIN=https://seu-frontend.com

# Forçar SSL no Postgres (geralmente não necessário com DATABASE_URL completa)
# PGSSL=true

# ====================================
# VARIÁVEIS ALTERNATIVAS (se não usar DATABASE_URL)
# ====================================
# Use estas APENAS se não configurar DATABASE_URL

# PGHOST=dpg-xxxx.oregon-postgres.render.com
# PGPORT=5432
# PGDATABASE=etica_vendas
# PGUSER=etica123
# PGPASSWORD=sua_senha_aqui
# PGSSL=true

# ====================================
# COMO CONFIGURAR NO RENDER
# ====================================
# 1. Acesse seu Web Service no dashboard
# 2. Vá em "Environment" na barra lateral
# 3. Clique em "Add Environment Variable"
# 4. Adicione cada variável (nome e valor)
# 5. Clique em "Save Changes"
# 6. O Render fará redeploy automático

# ====================================
# PARA GERAR JWT_SECRET
# ====================================
# Execute no PowerShell:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
