# Sample environment variables for local development
# Copy to .env and adjust before running locally. Never commit your real secrets.

PORT=3000
PGHOST=localhost
PGPORT=5432
PGDATABASE=etica_vendas
PGUSER=postgres
PGPASSWORD=postgres
PGSSL=false
JWT_SECRET=changeme-dev

# Alternatively you can set a single DATABASE_URL (preferred in production):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/etica_vendas
# If provider requires SSL (e.g. Render), also set:
# PGSSL=true

# CORS (apenas se hospedar front e backend separados)
# Ative CORS definindo CORS_ENABLED=true e configure a origem do front-end
# CORS_ENABLED=false
# FRONTEND_ORIGIN=https://seu-usuario.github.io
